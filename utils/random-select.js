//

const array = ['perro','gato', 'pajaro','raqueta' ]

module.exports.getRandomLabels = (labelsArray) => {
    const randomLabelsArray = [];
    // hacer funcion que elija X numero de items del array de manera aleatoria
    return randomLabelsArray
}

